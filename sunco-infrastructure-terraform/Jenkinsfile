pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Verify Workspace') {
            steps {
                sh '''
                    echo "Jenkins workspace:"
                    pwd

                    echo "Repository files:"
                    ls -la
                '''
            }
        }
    }
}
